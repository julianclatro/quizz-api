import { ethers } from 'ethers';

export class Auth {
  static async new(address: string, env: any){
    // const token = await sha256(crypto.randomUUID())
    // const user = await Auth.userExists(email, env)
  }
}